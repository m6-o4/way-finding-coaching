services:
  payload-template:
    image: ghcr.io/m6o4/payload-template:latest
    container_name: payload-template
    restart: unless-stopped
    networks:
      - m6o4-prd-network
    env_file:
      - stack.env
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.payload-template.rule=Host(`payload-template.s3.co.ke`)"
      - "traefik.http.routers.payload-template.entrypoints=websecure"
      - "traefik.http.routers.payload-template.tls.certresolver=letsencrypt"
      - "traefik.http.services.payload-template.loadbalancer.server.port=3000"

networks:
  m6o4-prd-network:
    external: true
    name: m6o4-prd-network
